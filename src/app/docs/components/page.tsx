import ComponentsDocsClient from "./ComponentsDocsClient";

export const metadata = {
  title: "Components — Nexkor Design System",
};

export default function ComponentsDocsPage() {
  return <ComponentsDocsClient />;
}
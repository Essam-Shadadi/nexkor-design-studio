import FoundationsDocsClient from "./FoundationsDocsClient";

export const metadata = {
  title: "Foundations — Nexkor Design System",
};

export default function FoundationsPage() {
  return <FoundationsDocsClient />;
}